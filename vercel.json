{
  "version": 2,
  "buildCommand": "cd smart-attendance/frontend && npm install && npm run build",
  "outputDirectory": "smart-attendance/frontend/build",
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
